# Examples

This folder contains public-safe example clients with placeholders only.

- `python_client.py` — basic POST example (set `COREPACT_API_URL` and `COREPACT_API_KEY`)
- `js_client.js` — basic fetch example (set env vars)
- `curl_example.txt` — one-liner to try locally (placeholders)

